optimizations:
    freeb -> embutido -> em vez de ser separada está embutida no forward pointer


passo forward pointer

malloc fora do list add first
passa antes o forward ponter (List Node)


ok the idea is freeb points too the first free block. then each free block points to the next.
might be a good idea to point to the next equal-or-bigger simmilar to what is being done already.
then it always occupies the first space that accomodates the nbytes.

so remove list completely.

at sweep:
    -> iterate from freeb until free block bigger than our one. change foward pointers so it fits in middle.
    -> if gets to end point to null and last that was there point to it.

when allocating:
    -> iterate from freeb until the first free block that accomodates the nbytes
